CONST word const_word = "my Pex"
CONST int const_val = 2023

add(int x, int y){
	int z;
	z = x + y;
}

spinCyle(){
	float var; 
	var = 20.23;

	if(var > const_val){
	int new;
	new = 100000 * const_val;
	}

	while(((x + 5 + 6) OR ((7 * 2 / 8) + var * 2)) == 1){
	word = word + "is almost done";
	var = var - 1;
	}
}

main(){
int foo;
int baz;
foo = 20;
baz = 23;
add(foo, baz);
spinCycle();

word program;
program = done;
}